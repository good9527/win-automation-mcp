[project]
name = "win-automation-mcp"
version = "0.1.0"
description = "Windows desktop automation MCP server"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.26.0",
    "comtypes>=1.4.0",
    "pillow>=10.0.0",
    "pyautogui>=0.9.54",
]

[project.scripts]
win-automation-mcp = "server:main"
